# Copy to keystore.properties (gitignored) OR prefer env via:
#   source /etc/plexi/secrets/plexi-wine-release.env
#
# storeFile=/etc/plexi/secrets/plexi-wine-release.keystore
# storePassword=***
# keyAlias=plexi-beer
# keyPassword=***
